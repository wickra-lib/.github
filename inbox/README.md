# Inbox

Generated by [`scripts/fetch-inbox.mjs`](../scripts/fetch-inbox.mjs) -- do not edit by hand.

Last scan: **2026-09-19T21:40:05Z** over 77 repositories. All three lists hold only what is open now.

## Dependabot pull requests with finished checks (2)

| Repository | PR | Checks | Age | Title |
|---|---|---|---|---|
| `wickra-backtest-live` | [#12](https://github.com/wickra-lib/wickra-backtest-live/pull/12) | green (2) | 2 days | deps(npm): bump vite from 8.2.2 to 8.3.0 in the site group |
| `wickra-live` | [#15](https://github.com/wickra-lib/wickra-live/pull/15) | green (3) | 2 days | deps(npm): bump vite from 8.2.2 to 8.3.0 in the site group |

## Issues and pull requests opened by people outside the org (1)

| Repository | Item | Author | Age | Title |
|---|---|---|---|---|
| `wickra-playground` | [pull request #13](https://github.com/wickra-lib/wickra-playground/pull/13) | @kingchenc | 2 days | deps: vitest 5, @types/node 26.5.1, eslint-plugin-vue 10.11.0; TypeScript stays on 6 |

## Open security alerts (0)

Code scanning, Dependabot and secret scanning alerts, read with GITHUB_TOKEN.

Not readable with this token, so not counted: `.github` (Dependabot, secret scanning), `webpage` (code scanning, Dependabot, secret scanning), `wickra` (code scanning, Dependabot, secret scanning), `wickra-backtest` (code scanning, Dependabot, secret scanning), `wickra-backtest-go` (code scanning, Dependabot, secret scanning), `wickra-backtest-live` (code scanning, Dependabot, secret scanning), `wickra-backtest-site` (code scanning, Dependabot, secret scanning), `wickra-benchmark` (code scanning, Dependabot, secret scanning), `wickra-benchmark-go` (code scanning, Dependabot, secret scanning), `wickra-benchmark-site` (code scanning, Dependabot, secret scanning), `wickra-compile` (code scanning, Dependabot, secret scanning), `wickra-compile-go` (code scanning, Dependabot, secret scanning), `wickra-compile-site` (code scanning, Dependabot, secret scanning), `wickra-copilot` (code scanning, Dependabot, secret scanning), `wickra-copilot-go` (code scanning, Dependabot, secret scanning), `wickra-copilot-site` (code scanning, Dependabot, secret scanning), `wickra-darwin` (code scanning, Dependabot, secret scanning), `wickra-darwin-go` (code scanning, Dependabot, secret scanning), `wickra-darwin-site` (code scanning, Dependabot, secret scanning), `wickra-docs` (code scanning, Dependabot, secret scanning), `wickra-embed` (code scanning, Dependabot, secret scanning), `wickra-embed-site` (code scanning, Dependabot, secret scanning), `wickra-exchange` (code scanning, Dependabot, secret scanning), `wickra-exchange-go` (code scanning, Dependabot, secret scanning), `wickra-exchange-site` (code scanning, Dependabot, secret scanning), `wickra-feature-store` (code scanning, Dependabot, secret scanning), `wickra-feature-store-go` (code scanning, Dependabot, secret scanning), `wickra-feature-store-site` (code scanning, Dependabot, secret scanning), `wickra-genome` (code scanning, Dependabot, secret scanning), `wickra-genome-go` (code scanning, Dependabot, secret scanning), `wickra-genome-site` (code scanning, Dependabot, secret scanning), `wickra-go` (code scanning, Dependabot, secret scanning), `wickra-gym` (code scanning, Dependabot, secret scanning), `wickra-gym-go` (code scanning, Dependabot, secret scanning), `wickra-gym-site` (code scanning, Dependabot, secret scanning), `wickra-impact` (code scanning, Dependabot, secret scanning), `wickra-impact-go` (code scanning, Dependabot, secret scanning), `wickra-impact-site` (code scanning, Dependabot, secret scanning), `wickra-lib.r-universe.dev` (code scanning, Dependabot, secret scanning), `wickra-live` (code scanning, Dependabot, secret scanning), `wickra-pico` (code scanning, Dependabot, secret scanning), `wickra-pico-site` (code scanning, Dependabot, secret scanning), `wickra-playground` (code scanning, Dependabot, secret scanning), `wickra-playground-site` (code scanning, Dependabot, secret scanning), `wickra-proof` (code scanning, Dependabot, secret scanning), `wickra-proof-go` (code scanning, Dependabot, secret scanning), `wickra-proof-site` (code scanning, Dependabot, secret scanning), `wickra-radar` (code scanning, Dependabot, secret scanning), `wickra-radar-go` (code scanning, Dependabot, secret scanning), `wickra-radar-site` (code scanning, Dependabot, secret scanning), `wickra-screener` (code scanning, Dependabot, secret scanning), `wickra-screener-go` (code scanning, Dependabot, secret scanning), `wickra-screener-site` (code scanning, Dependabot, secret scanning), `wickra-shazam` (code scanning, Dependabot, secret scanning), `wickra-shazam-go` (code scanning, Dependabot, secret scanning), `wickra-shazam-site` (code scanning, Dependabot, secret scanning), `wickra-strategy-ci` (code scanning, Dependabot, secret scanning), `wickra-strategy-ci-go` (code scanning, Dependabot, secret scanning), `wickra-strategy-ci-site` (code scanning, Dependabot, secret scanning), `wickra-synth` (code scanning, Dependabot, secret scanning), `wickra-synth-go` (code scanning, Dependabot, secret scanning), `wickra-synth-site` (code scanning, Dependabot, secret scanning), `wickra-terminal` (code scanning, Dependabot, secret scanning), `wickra-terminal-go` (code scanning, Dependabot, secret scanning), `wickra-terminal-site` (code scanning, Dependabot, secret scanning), `wickra-timemachine` (code scanning, Dependabot, secret scanning), `wickra-timemachine-go` (code scanning, Dependabot, secret scanning), `wickra-timemachine-site` (code scanning, Dependabot, secret scanning), `wickra-verify` (code scanning, Dependabot, secret scanning), `wickra-verify-go` (code scanning, Dependabot, secret scanning), `wickra-verify-site` (code scanning, Dependabot, secret scanning), `wickra-xray` (code scanning, Dependabot, secret scanning), `wickra-xray-go` (code scanning, Dependabot, secret scanning), `wickra-xray-site` (code scanning, Dependabot, secret scanning), `wickra-zk` (code scanning, Dependabot, secret scanning), `wickra-zk-go` (code scanning, Dependabot, secret scanning), `wickra-zk-site` (code scanning, Dependabot, secret scanning). Set INBOX_TOKEN to a token with read access to the three alert lists on the org's repositories.

None.

